{
  "name": "@use-tusk/drift-core-node",
  "version": "0.1.5",
  "description": "Node bindings for drift-rust-core",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "clean": "rm -rf target *.node"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
